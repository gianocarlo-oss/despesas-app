<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MeuApp"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".notas.NotasActivity" android:exported="false" android:label="Bloco de Notas" />
        <activity android:name=".notas.NotaEditActivity" android:exported="false" android:label="Nota" />

        <activity android:name=".agenda.AgendaActivity" android:exported="false" android:label="Agenda" />
        <activity android:name=".agenda.ContatoEditActivity" android:exported="false" android:label="Contato" />

        <activity android:name=".despesas.DespesasActivity" android:exported="false" android:label="Controle de Gastos" />

    </application>
</manifest>
